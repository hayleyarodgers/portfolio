import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';
import 'bootstrap/dist/css/bootstrap.min.css';

// Renders all components to the DOM at the root level.
ReactDOM.render(<App />, document.getElementById('root'));
